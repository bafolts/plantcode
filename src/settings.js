
if (typeof define !== 'function') {
    var define = require('amdefine')(module);
}

define(function (require) {
    return {
        languages: ["coffeescript", "csharp", "ecmascript5", "ecmascript6", "java", "php", "ruby", "typescript"]
    }
});
