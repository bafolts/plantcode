{
  "name": "plantcode",
  "version": "0.0.1",
  "description": "PlantUML to class file generator.",
  "author": "Brian Folts <brian.folts@gmail.com>",
  "main": "plantcode",
  "scripts": {
    "test": "plantcode -l java tests/car.pegjs > tests/car.java && plantcode -l coffeescript tests/car.pegjs > tests/car.coffee && plantcode -l typescript tests/car.pegjs > tests/car.ts && plantcode -l ruby tests/car.pegjs > tests/car.rb && plantcode -l php tests/car.pegjs > tests/car.php"
  },
  "bin": "plantcode",
  "repository": {
    "type": "git",
    "url": "git://github.com/bafolts/plantuml-code-generator.git"
  },
  "dependencies": {
    "handlebars": "*"
  },
  "engines": {
    "node": ">= 0.8"
  }
}